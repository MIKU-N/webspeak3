{
  "name": "webspeak3-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "WebSpeak3 gateway server: bridges browser (WebSocket) clients to a native TeamSpeak server",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
